# Dependencias del backend. Uso: pip install -r requirements.txt (desde raíz) o pip install -r backend/requirements.txt
# Proyecto migrado a Live Avatar; sin HeyGen Streaming ni STT local.
fastapi
uvicorn[standard]
requests
openai
python-dotenv
python-multipart
websockets>=12.0
